@model Zadanie.Models.FormModel
@{
    ViewData["Title"] = "Form";
}

<div class="text-left">
    <h1 class="display-4">Wynik formularza</h1>
    @Html.DisplayNameFor(model => model.imie):          @Html.DisplayFor(model => model.imie)
    <br />
   
    <p> ilość_powtorzeń @Html.DisplayFor(model => model.belongs) do przedziału 1..5 </p>

    @for (int i = 0; i <= Model.ilosc_powtorzen; i++)
    {
        <p>@Model.imie</p>
    }
   
</div>
